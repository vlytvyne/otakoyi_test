import 'package:flutter/material.dart';

class WomenFragment extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) =>
      Center(
        child: Text(
          'WOMEN'
        ),
      );
  
}